package sheen.leetcode.array;

/**
 * @Classname 摘水果
 * @Date 2021/12/12 11:08 上午
 * @Created by sheen
 * @Description TODO
 */
public class 摘水果 {
    /*public int maxTotalFruits(int[][] fruits, int startPos, int k) {
        int[] arr = new int[200001];
        for(int i = 0; i < fruits.length; i++) {
            arr[fruits[i][0]] = fruits[i][1];
        }
        for(int i = 1; i < arr.length; i++) {
            arr[i] += arr[i-1];
        }

        if(startPos == 0) {
            return arr[Math.min(k, arr.length)];
        }
        if(startPos == arr.length - 1) {
            int end = startPos - k;

        }
        for(int i = 0; i <= k; i++) {
            if(startPos == 0) {

            }

        }
    }*/
}
